# Tg_bot/handlers/info.py

from typing import Any

from telegram.ext import ConversationHandler, CallbackContext

# Conversation Handler Placeholder

info_conversation: ConversationHandler[Any] = ConversationHandler(
    entry_points=[],
    states={},
    fallbacks=[]
)
